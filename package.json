{
	"private": true,
	"license": "MIT",
	"workspaces": [
		"runtime",
		"transformer"
	],
	"scripts": {
		"build:transformer": "npx tsc --p transformer/tsconfig.json ",
		"build:runtime": "npx tsc --p runtime/tsconfig.json ",
		"build": "npm run build:transformer && npm run build:runtime"
	},
	"engines": {
		"npm": ">=7.10.0",
		"node": ">=16.0.0"
	},
	"devDependencies": {
		"@types/node": "^16.0.0",
		"ttypescript": "1.x",
		"typescript": "4.x",
		"ts-node": "^10.4.0"
	}
}
